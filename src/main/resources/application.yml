server:
  port: 8001

spring:
  config:
    import: classpath:logging.yml

  datasource:
    url: jdbc:postgresql://localhost:5433/line_bot
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql

  flyway:
    url: jdbc:postgresql://localhost:5433/line_bot
    user: postgres
    password: postgres
